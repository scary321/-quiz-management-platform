# Leave unset in development: Vite proxies /api to http://localhost:8000.
# Set this for a deployed frontend that talks to a separate backend host.
# VITE_API_URL=https://your-api-host.onrender.com/api
